[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["physics", "datasets", "models", "nsbi", "tools", "hpo"]

[project]
name = "hstar-trilinear"
version = "1.0.0"